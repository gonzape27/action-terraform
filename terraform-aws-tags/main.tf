locals {

	all_tags = {

	  environment  = var.environment
	  project_name = var.project_name
	  module_name  = var.module_name
	  owner		   = var.owner
	  email    	   = var.email
	  name    	   = var.name
    }
}


